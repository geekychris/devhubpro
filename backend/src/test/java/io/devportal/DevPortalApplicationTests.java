package io.devportal;

import org.junit.jupiter.api.Test;

class DevPortalApplicationTests {
    @Test
    void contextLoadsPlaceholder() {
        // Real @SpringBootTest context-load test added once Postgres is wired in CI.
        // For now we keep a passing test so the build pipeline has at least one test target.
    }
}
